<?xml version="1.0" encoding="utf-8" ?>
<mapper namespace="weed3demo.xmlsql2" :db="testdb">
    <sql id="user_add" :return="long"
         :param="m:weed3demo.mapper.UserModel,sex:int"
         :note="添加用户"
         :cacheClear="user_${m.user_id}"><![CDATA[
        INSERT user(user_id,mobile,sex) VALUES(@{m.user_id},@{m.mobile},@{sex})
    ]]></sql>

    <sql id="user_add3" :return="long"
         :param="m:weed3demo.mapper.UserModel,sex:int"
         :note="添加用户"
         :cacheClear="user_${m.user_id}">
        INSERT user
        <trim prefix="(" suffix=")" trimEnd=",">
            user_id,sex,
            <if test="m.mobile != null">
                mobile,
            </if>
            <if test="m.icon != null">
                icon,
            </if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" trimEnd=",">
            @{m.user_id},@{sex},
            <if test="m.mobile != null">
                @{m.mobile},
            </if>
            <if test="m.icon != null">
                @{m.icon},
            </if>
        </trim>
    </sql>

    <sql id="user_add_for" :return="long" :note="批量添加用户3">
        INSERT user(id,mobile,sex) VALUES
        <for var="m:weed3demo.mapper.UserModel" sep="#" items="list">
            (@{m.user_id},@{m.mobile},@{m.sex})
        </for>
    </sql>


    <sql id="user_set" :return="long" :note="更新用户"><![CDATA[
        UPDATE user SET sex=1 WHERE id IN(@{user_list:List[Integer]})
    ]]></sql>

    <sql id="user_add_for2" :return="long" :note="批量添加用户3">
        INSERT user(id,mobile,sex) VALUES
        <for var="m:weed3demo.mapper.UserModel" items="list">
            <if test="m.users != null">
                <for var="m2:weed3demo.mapper.UserModel" items="m.users">
                    (@{m2.user_id},@{m2.mobile},@{m2.sex})
                </for>
            </if>
        </for>
    </sql>
</mapper>